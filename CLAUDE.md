<!--
  Intentionally minimal. All agent instructions live in AGENTS.md so that Claude Code and
  other coding agents read the same file — edit AGENTS.md, never duplicate rules here.
  The @-line below is a Claude Code import: it pulls AGENTS.md into context at session
  start. This HTML comment is stripped before injection, so it costs no tokens.
-->
@AGENTS.md
